services:
  nitelite-pipeline:
    image: nitelite-pipeline
  postgis:
    image: postgis/postgis
    restart: always
    # build:
      # context: ..
      # dockerfile: ./build/postgis.Dockerfile
    environment:
      - POSTGRES_DB=gis
      - POSTGRES_USER=gis
      - POSTGRES_PASSWORD=gis
    ports:
      - 5432:5432
    volumes:
      - /Users/Shared/data/nitelite/220513-FH135/postgis:/var/lib/postgresql/data
